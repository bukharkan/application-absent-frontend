export default class User {
    constructor(divisi, email, fullname, password) {
        this.divisi = divisi;
        this.email = email;
        this.fullname = fullname;
        this.password = password;
    }
}